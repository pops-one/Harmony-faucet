{
  "name": "harmony-erc20",
  "version": "1.0.0",
  "description": "Harmony sample ERC20 deploy",
  "main": "truffle.js",
  "scripts": {
    "deploy": "truffle migrate --network testnet --reset",
    "copy": "cp ./build/contracts/HarmonyFaucet.json ../faucet-backend/config",
    "build": "npm run deploy && npm run copy"
  },
  "dependencies": {
    "@harmony-js/core": "^0.1.49",
    "@openzeppelin/contracts": "^2.5.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "truffle": "^5.1.35",
    "tslib": "^1.10.0"
  }
}
